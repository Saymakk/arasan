import 'package:flutter/material.dart';

class SertificatesScreen extends StatelessWidget {
  const SertificatesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
