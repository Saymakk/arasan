import 'package:flutter/material.dart';

class AbonementsScreen extends StatelessWidget {
  const AbonementsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
