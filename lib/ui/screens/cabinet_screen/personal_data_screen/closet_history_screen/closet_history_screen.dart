import 'package:flutter/material.dart';

class ClosetHistoryScreen extends StatelessWidget {
  const ClosetHistoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
